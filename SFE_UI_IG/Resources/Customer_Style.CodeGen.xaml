<!--
 WARNING: This code was generated by CodeGen. Any changes that you
          make to this code will be overwritten if the code is regenerated!

 Template author:   Richard C. Morris, Synergex Professional Services Group

 Template Name: Symphony Framework : SYMPHONY_STYLE_IG_V3_2.tpl
 
-->
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:clr="clr-namespace:System;assembly=mscorlib"
    xmlns:igWPF="http://schemas.infragistics.com/xaml/wpf"
    xmlns:symphonyConverters="clr-namespace:Symphony.Conductor.Converters;assembly=SymphonyConductor"
    xmlns:symphonyTriggers="clr-namespace:Symphony.Conductor.Triggers;assembly=SymphonyConductor"
    xmlns:symphonyUI="clr-namespace:Symphony.Crescendo.Presentation;assembly=SymphonyCrescendo"
    xmlns:symphonyControls="clr-namespace:Symphony.Conductor.Controls;assembly=SymphonyConductor"
	xmlns:uiControls="clr-namespace:SFE_UI.Controls;assembly=SFE_UI"
    xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/SymphonyConductor;component/Resources/Styles.xaml"/>
        <ResourceDictionary Source="pack://application:,,,/SymphonyConductor;component/Resources/Converters.xaml"/>
        <ResourceDictionary Source="pack://application:,,,/SFE_UI;component/Resources/Customer_Content.CodeGen.xaml"/>
     </ResourceDictionary.MergedDictionaries>


    <clr:String x:Key="Customer_Cust_code_heading">Customer Code</clr:String>


    <Style x:Key="Customer_Cust_code_prompt" TargetType="{x:Type Label}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Label}">
                    <Label
                        Content="Customer Code"
                        IsEnabled="{Binding Path=Cust_codeIsEnabled}">
                    </Label>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>



    <Style x:Key="Customer_Cust_code_style" TargetType="{x:Type symphonyControls:FieldControl}">
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Focusable" Value="False"></Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type symphonyControls:FieldControl}">
                    <igWPF:XamNumericEditor
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        Name="ctlCustomer_Cust_code"
                        Value="{Binding Path=Cust_code
                        , Converter={StaticResource nullConverter}  
                        ,UpdateSourceTrigger=PropertyChanged
                        ,ValidatesOnDataErrors=True}"
                        ValueType="{x:Type clr:Int32}"
                        Mask="{}{char:6:0-9}"
                        Validation.ErrorTemplate="{StaticResource validationTemplate}"
                        IsEnabled="{Binding Path=Cust_codeIsEnabled}"
                        IsReadOnly="{Binding Path=Cust_codeIsReadOnly}"
                        Width="72"
                        ToolTip="{Binding RelativeSource={RelativeSource Self},Path=(Validation.Errors), Converter={StaticResource errorConveter}}">
						<i:Interaction.Behaviors>
							<uiControls:SelectAllNumericEditorBehavior></uiControls:SelectAllNumericEditorBehavior>
						</i:Interaction.Behaviors>
                        <igWPF:XamNumericEditor.ValueConstraint>
                            <igWPF:ValueConstraint MaxLength="6"></igWPF:ValueConstraint>
                        </igWPF:XamNumericEditor.ValueConstraint>
                        <igWPF:XamNumericEditor.Style>
                            <Style>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Path=Cust_codeIsFocused}" Value="true">
                                        <Setter Property="FocusManager.FocusedElement"
                                                Value="{Binding ElementName=ctlCustomer_Cust_code}"></Setter>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self},Path=(Validation.HasError)}" Value="True">
                                        <Setter Property="TextBox.Background">
                                            <Setter.Value>
                                                <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                                                    <LinearGradientBrush.GradientStops>
                                                        <GradientStop Offset="0.2" Color="WhiteSmoke" />
                                                        <GradientStop Offset="3" Color="Red" />
                                                    </LinearGradientBrush.GradientStops>
                                                </LinearGradientBrush>
                                            </Setter.Value>
                                        </Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </igWPF:XamNumericEditor.Style>
                    </igWPF:XamNumericEditor>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    

    <clr:String x:Key="Customer_Name_heading">Supplier Name</clr:String>


    <Style x:Key="Customer_Name_prompt" TargetType="{x:Type Label}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Label}">
                    <Label
                        Content="Supplier Name"
                        IsEnabled="{Binding Path=NameIsEnabled}">
                    </Label>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>



    <Style x:Key="Customer_Name_style" TargetType="{x:Type symphonyControls:FieldControl}">
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Focusable" Value="False"></Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type symphonyControls:FieldControl}">
                    <igWPF:XamMaskedEditor
                        Name="ctlCustomer_Name"
                        Value="{Binding Path=Name, UpdateSourceTrigger=LostFocus,
                        ValidatesOnDataErrors=True}"
                        Validation.ErrorTemplate="{StaticResource validationTemplate}"
                        Width="360"
                        IsEnabled="{Binding Path=NameIsEnabled}"
                        IsReadOnly="{Binding Path=NameIsReadOnly}"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Left"
                        ToolTip="{Binding RelativeSource={RelativeSource Self},Path=(Validation.Errors), Converter={StaticResource errorConveter}}">
						<i:Interaction.Behaviors>
							<uiControls:SelectAllMaskedEditorBehavior></uiControls:SelectAllMaskedEditorBehavior>
						</i:Interaction.Behaviors>
                        <igWPF:XamMaskedEditor.ValueConstraint>
                            <igWPF:ValueConstraint MaxLength="30"></igWPF:ValueConstraint>
                        </igWPF:XamMaskedEditor.ValueConstraint>
                        <igWPF:XamMaskedEditor.Style>
                            <Style>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Path=NameIsFocused}" Value="true">
                                        <Setter Property="FocusManager.FocusedElement"
                                                Value="{Binding ElementName=ctlCustomer_Name}"></Setter>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self},Path=(Validation.HasError)}" Value="True">
                                        <Setter Property="TextBox.Background">
                                            <Setter.Value>
                                                <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                                                <LinearGradientBrush.GradientStops>
                                                    <GradientStop Offset="0.2" Color="WhiteSmoke" />
                                                    <GradientStop Offset="3" Color="Red" />
                                                </LinearGradientBrush.GradientStops>
                                                </LinearGradientBrush>
                                            </Setter.Value>
                                        </Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </igWPF:XamMaskedEditor.Style>
                    </igWPF:XamMaskedEditor>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    

    <clr:String x:Key="Customer_Address1_heading">Address 1</clr:String>


    <Style x:Key="Customer_Address1_prompt" TargetType="{x:Type Label}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Label}">
                    <Label
                        Content="Address"
                        IsEnabled="{Binding Path=Address1IsEnabled}">
                    </Label>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>



    <Style x:Key="Customer_Address1_style" TargetType="{x:Type symphonyControls:FieldControl}">
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Focusable" Value="False"></Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type symphonyControls:FieldControl}">
                    <igWPF:XamMaskedEditor
                        Name="ctlCustomer_Address1"
                        Value="{Binding Path=Address1, UpdateSourceTrigger=LostFocus,
                        ValidatesOnDataErrors=True}"
                        Validation.ErrorTemplate="{StaticResource validationTemplate}"
                        Width="360"
                        IsEnabled="{Binding Path=Address1IsEnabled}"
                        IsReadOnly="{Binding Path=Address1IsReadOnly}"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Left"
                        ToolTip="{Binding RelativeSource={RelativeSource Self},Path=(Validation.Errors), Converter={StaticResource errorConveter}}">
						<i:Interaction.Behaviors>
							<uiControls:SelectAllMaskedEditorBehavior></uiControls:SelectAllMaskedEditorBehavior>
						</i:Interaction.Behaviors>
                        <igWPF:XamMaskedEditor.ValueConstraint>
                            <igWPF:ValueConstraint MaxLength="30"></igWPF:ValueConstraint>
                        </igWPF:XamMaskedEditor.ValueConstraint>
                        <igWPF:XamMaskedEditor.Style>
                            <Style>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Path=Address1IsFocused}" Value="true">
                                        <Setter Property="FocusManager.FocusedElement"
                                                Value="{Binding ElementName=ctlCustomer_Address1}"></Setter>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self},Path=(Validation.HasError)}" Value="True">
                                        <Setter Property="TextBox.Background">
                                            <Setter.Value>
                                                <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                                                <LinearGradientBrush.GradientStops>
                                                    <GradientStop Offset="0.2" Color="WhiteSmoke" />
                                                    <GradientStop Offset="3" Color="Red" />
                                                </LinearGradientBrush.GradientStops>
                                                </LinearGradientBrush>
                                            </Setter.Value>
                                        </Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </igWPF:XamMaskedEditor.Style>
                    </igWPF:XamMaskedEditor>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    

    <clr:String x:Key="Customer_Address2_heading">Address 2</clr:String>


    <Style x:Key="Customer_Address2_prompt" TargetType="{x:Type Label}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Label}">
                    <Label
                        Content=""
                        IsEnabled="{Binding Path=Address2IsEnabled}">
                    </Label>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>



    <Style x:Key="Customer_Address2_style" TargetType="{x:Type symphonyControls:FieldControl}">
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Focusable" Value="False"></Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type symphonyControls:FieldControl}">
                    <igWPF:XamMaskedEditor
                        Name="ctlCustomer_Address2"
                        Value="{Binding Path=Address2, UpdateSourceTrigger=LostFocus,
                        ValidatesOnDataErrors=True}"
                        Validation.ErrorTemplate="{StaticResource validationTemplate}"
                        Width="360"
                        IsEnabled="{Binding Path=Address2IsEnabled}"
                        IsReadOnly="{Binding Path=Address2IsReadOnly}"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Left"
                        ToolTip="{Binding RelativeSource={RelativeSource Self},Path=(Validation.Errors), Converter={StaticResource errorConveter}}">
						<i:Interaction.Behaviors>
							<uiControls:SelectAllMaskedEditorBehavior></uiControls:SelectAllMaskedEditorBehavior>
						</i:Interaction.Behaviors>
                        <igWPF:XamMaskedEditor.ValueConstraint>
                            <igWPF:ValueConstraint MaxLength="30"></igWPF:ValueConstraint>
                        </igWPF:XamMaskedEditor.ValueConstraint>
                        <igWPF:XamMaskedEditor.Style>
                            <Style>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Path=Address2IsFocused}" Value="true">
                                        <Setter Property="FocusManager.FocusedElement"
                                                Value="{Binding ElementName=ctlCustomer_Address2}"></Setter>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self},Path=(Validation.HasError)}" Value="True">
                                        <Setter Property="TextBox.Background">
                                            <Setter.Value>
                                                <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                                                <LinearGradientBrush.GradientStops>
                                                    <GradientStop Offset="0.2" Color="WhiteSmoke" />
                                                    <GradientStop Offset="3" Color="Red" />
                                                </LinearGradientBrush.GradientStops>
                                                </LinearGradientBrush>
                                            </Setter.Value>
                                        </Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </igWPF:XamMaskedEditor.Style>
                    </igWPF:XamMaskedEditor>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    

    <clr:String x:Key="Customer_Address3_heading">Address 3</clr:String>


    <Style x:Key="Customer_Address3_prompt" TargetType="{x:Type Label}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Label}">
                    <Label
                        Content=""
                        IsEnabled="{Binding Path=Address3IsEnabled}">
                    </Label>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>



    <Style x:Key="Customer_Address3_style" TargetType="{x:Type symphonyControls:FieldControl}">
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Focusable" Value="False"></Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type symphonyControls:FieldControl}">
                    <igWPF:XamMaskedEditor
                        Name="ctlCustomer_Address3"
                        Value="{Binding Path=Address3, UpdateSourceTrigger=LostFocus,
                        ValidatesOnDataErrors=True}"
                        Validation.ErrorTemplate="{StaticResource validationTemplate}"
                        Width="360"
                        IsEnabled="{Binding Path=Address3IsEnabled}"
                        IsReadOnly="{Binding Path=Address3IsReadOnly}"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Left"
                        ToolTip="{Binding RelativeSource={RelativeSource Self},Path=(Validation.Errors), Converter={StaticResource errorConveter}}">
						<i:Interaction.Behaviors>
							<uiControls:SelectAllMaskedEditorBehavior></uiControls:SelectAllMaskedEditorBehavior>
						</i:Interaction.Behaviors>
                        <igWPF:XamMaskedEditor.ValueConstraint>
                            <igWPF:ValueConstraint MaxLength="30"></igWPF:ValueConstraint>
                        </igWPF:XamMaskedEditor.ValueConstraint>
                        <igWPF:XamMaskedEditor.Style>
                            <Style>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Path=Address3IsFocused}" Value="true">
                                        <Setter Property="FocusManager.FocusedElement"
                                                Value="{Binding ElementName=ctlCustomer_Address3}"></Setter>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self},Path=(Validation.HasError)}" Value="True">
                                        <Setter Property="TextBox.Background">
                                            <Setter.Value>
                                                <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                                                <LinearGradientBrush.GradientStops>
                                                    <GradientStop Offset="0.2" Color="WhiteSmoke" />
                                                    <GradientStop Offset="3" Color="Red" />
                                                </LinearGradientBrush.GradientStops>
                                                </LinearGradientBrush>
                                            </Setter.Value>
                                        </Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </igWPF:XamMaskedEditor.Style>
                    </igWPF:XamMaskedEditor>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    

    <clr:String x:Key="Customer_Postcode_heading">Post Code</clr:String>


    <Style x:Key="Customer_Postcode_prompt" TargetType="{x:Type Label}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Label}">
                    <Label
                        Content="Post Code"
                        IsEnabled="{Binding Path=PostcodeIsEnabled}">
                    </Label>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>



    <Style x:Key="Customer_Postcode_style" TargetType="{x:Type symphonyControls:FieldControl}">
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Focusable" Value="False"></Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type symphonyControls:FieldControl}">
                    <igWPF:XamMaskedEditor
                        Name="ctlCustomer_Postcode"
                        Value="{Binding Path=Postcode, UpdateSourceTrigger=LostFocus,
                        ValidatesOnDataErrors=True}"
                        Validation.ErrorTemplate="{StaticResource validationTemplate}"
                        Width="120"
                        IsEnabled="{Binding Path=PostcodeIsEnabled}"
                        IsReadOnly="{Binding Path=PostcodeIsReadOnly}"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Left"
                        ToolTip="{Binding RelativeSource={RelativeSource Self},Path=(Validation.Errors), Converter={StaticResource errorConveter}}">
						<i:Interaction.Behaviors>
							<uiControls:SelectAllMaskedEditorBehavior></uiControls:SelectAllMaskedEditorBehavior>
						</i:Interaction.Behaviors>
                        <igWPF:XamMaskedEditor.ValueConstraint>
                            <igWPF:ValueConstraint MaxLength="10"></igWPF:ValueConstraint>
                        </igWPF:XamMaskedEditor.ValueConstraint>
                        <igWPF:XamMaskedEditor.Style>
                            <Style>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Path=PostcodeIsFocused}" Value="true">
                                        <Setter Property="FocusManager.FocusedElement"
                                                Value="{Binding ElementName=ctlCustomer_Postcode}"></Setter>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self},Path=(Validation.HasError)}" Value="True">
                                        <Setter Property="TextBox.Background">
                                            <Setter.Value>
                                                <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                                                <LinearGradientBrush.GradientStops>
                                                    <GradientStop Offset="0.2" Color="WhiteSmoke" />
                                                    <GradientStop Offset="3" Color="Red" />
                                                </LinearGradientBrush.GradientStops>
                                                </LinearGradientBrush>
                                            </Setter.Value>
                                        </Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </igWPF:XamMaskedEditor.Style>
                    </igWPF:XamMaskedEditor>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    

    <clr:String x:Key="Customer_Phone_heading">Phone No</clr:String>


    <Style x:Key="Customer_Phone_prompt" TargetType="{x:Type Label}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Label}">
                    <Label
                        Content="Phone Number"
                        IsEnabled="{Binding Path=PhoneIsEnabled}">
                    </Label>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>



    <Style x:Key="Customer_Phone_style" TargetType="{x:Type symphonyControls:FieldControl}">
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Focusable" Value="False"></Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type symphonyControls:FieldControl}">
                    <igWPF:XamMaskedEditor
                        Name="ctlCustomer_Phone"
                        Value="{Binding Path=Phone, UpdateSourceTrigger=LostFocus,
                        ValidatesOnDataErrors=True}"
                        Validation.ErrorTemplate="{StaticResource validationTemplate}"
                        Width="240"
                        IsEnabled="{Binding Path=PhoneIsEnabled}"
                        IsReadOnly="{Binding Path=PhoneIsReadOnly}"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Left"
                        ToolTip="{Binding RelativeSource={RelativeSource Self},Path=(Validation.Errors), Converter={StaticResource errorConveter}}">
						<i:Interaction.Behaviors>
							<uiControls:SelectAllMaskedEditorBehavior></uiControls:SelectAllMaskedEditorBehavior>
						</i:Interaction.Behaviors>
                        <igWPF:XamMaskedEditor.ValueConstraint>
                            <igWPF:ValueConstraint MaxLength="20"></igWPF:ValueConstraint>
                        </igWPF:XamMaskedEditor.ValueConstraint>
                        <igWPF:XamMaskedEditor.Style>
                            <Style>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Path=PhoneIsFocused}" Value="true">
                                        <Setter Property="FocusManager.FocusedElement"
                                                Value="{Binding ElementName=ctlCustomer_Phone}"></Setter>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self},Path=(Validation.HasError)}" Value="True">
                                        <Setter Property="TextBox.Background">
                                            <Setter.Value>
                                                <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                                                <LinearGradientBrush.GradientStops>
                                                    <GradientStop Offset="0.2" Color="WhiteSmoke" />
                                                    <GradientStop Offset="3" Color="Red" />
                                                </LinearGradientBrush.GradientStops>
                                                </LinearGradientBrush>
                                            </Setter.Value>
                                        </Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </igWPF:XamMaskedEditor.Style>
                    </igWPF:XamMaskedEditor>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    

    <clr:String x:Key="Customer_Contact_heading">Contact Name</clr:String>


    <Style x:Key="Customer_Contact_prompt" TargetType="{x:Type Label}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Label}">
                    <Label
                        Content="Contact Name"
                        IsEnabled="{Binding Path=ContactIsEnabled}">
                    </Label>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>



    <Style x:Key="Customer_Contact_style" TargetType="{x:Type symphonyControls:FieldControl}">
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Focusable" Value="False"></Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type symphonyControls:FieldControl}">
                    <igWPF:XamMaskedEditor
                        Name="ctlCustomer_Contact"
                        Value="{Binding Path=Contact, UpdateSourceTrigger=LostFocus,
                        ValidatesOnDataErrors=True}"
                        Validation.ErrorTemplate="{StaticResource validationTemplate}"
                        Width="360"
                        IsEnabled="{Binding Path=ContactIsEnabled}"
                        IsReadOnly="{Binding Path=ContactIsReadOnly}"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Left"
                        ToolTip="{Binding RelativeSource={RelativeSource Self},Path=(Validation.Errors), Converter={StaticResource errorConveter}}">
						<i:Interaction.Behaviors>
							<uiControls:SelectAllMaskedEditorBehavior></uiControls:SelectAllMaskedEditorBehavior>
						</i:Interaction.Behaviors>
                        <igWPF:XamMaskedEditor.ValueConstraint>
                            <igWPF:ValueConstraint MaxLength="30"></igWPF:ValueConstraint>
                        </igWPF:XamMaskedEditor.ValueConstraint>
                        <igWPF:XamMaskedEditor.Style>
                            <Style>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Path=ContactIsFocused}" Value="true">
                                        <Setter Property="FocusManager.FocusedElement"
                                                Value="{Binding ElementName=ctlCustomer_Contact}"></Setter>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self},Path=(Validation.HasError)}" Value="True">
                                        <Setter Property="TextBox.Background">
                                            <Setter.Value>
                                                <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                                                <LinearGradientBrush.GradientStops>
                                                    <GradientStop Offset="0.2" Color="WhiteSmoke" />
                                                    <GradientStop Offset="3" Color="Red" />
                                                </LinearGradientBrush.GradientStops>
                                                </LinearGradientBrush>
                                            </Setter.Value>
                                        </Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </igWPF:XamMaskedEditor.Style>
                    </igWPF:XamMaskedEditor>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    

    <clr:String x:Key="Customer_Email_heading">Email</clr:String>


    <Style x:Key="Customer_Email_prompt" TargetType="{x:Type Label}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Label}">
                    <Label
                        Content="Email Address"
                        IsEnabled="{Binding Path=EmailIsEnabled}">
                    </Label>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>



    <Style x:Key="Customer_Email_style" TargetType="{x:Type symphonyControls:FieldControl}">
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Focusable" Value="False"></Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type symphonyControls:FieldControl}">
                    <igWPF:XamMaskedEditor
                        Name="ctlCustomer_Email"
                        Value="{Binding Path=Email, UpdateSourceTrigger=LostFocus,
                        ValidatesOnDataErrors=True}"
                        Validation.ErrorTemplate="{StaticResource validationTemplate}"
                        Width="600"
                        IsEnabled="{Binding Path=EmailIsEnabled}"
                        IsReadOnly="{Binding Path=EmailIsReadOnly}"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Left"
                        ToolTip="{Binding RelativeSource={RelativeSource Self},Path=(Validation.Errors), Converter={StaticResource errorConveter}}">
						<i:Interaction.Behaviors>
							<uiControls:SelectAllMaskedEditorBehavior></uiControls:SelectAllMaskedEditorBehavior>
						</i:Interaction.Behaviors>
                        <igWPF:XamMaskedEditor.ValueConstraint>
                            <igWPF:ValueConstraint MaxLength="50"></igWPF:ValueConstraint>
                        </igWPF:XamMaskedEditor.ValueConstraint>
                        <igWPF:XamMaskedEditor.Style>
                            <Style>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Path=EmailIsFocused}" Value="true">
                                        <Setter Property="FocusManager.FocusedElement"
                                                Value="{Binding ElementName=ctlCustomer_Email}"></Setter>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self},Path=(Validation.HasError)}" Value="True">
                                        <Setter Property="TextBox.Background">
                                            <Setter.Value>
                                                <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                                                <LinearGradientBrush.GradientStops>
                                                    <GradientStop Offset="0.2" Color="WhiteSmoke" />
                                                    <GradientStop Offset="3" Color="Red" />
                                                </LinearGradientBrush.GradientStops>
                                                </LinearGradientBrush>
                                            </Setter.Value>
                                        </Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </igWPF:XamMaskedEditor.Style>
                    </igWPF:XamMaskedEditor>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    

    <clr:String x:Key="Customer_Create_date_heading">Creation date</clr:String>


    <Style x:Key="Customer_Create_date_prompt" TargetType="{x:Type Label}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Label}">
                    <Label
                        Content="Creation date"
                        IsEnabled="{Binding Path=Create_dateIsEnabled}">
                    </Label>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>



    <Style x:Key="Customer_Create_date_style" TargetType="{x:Type symphonyControls:FieldControl}">
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Focusable" Value="False"></Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type symphonyControls:FieldControl}">
                    <igWPF:XamDateTimeEditor
                        HorizontalAlignment="Left"
                        Name="ctlCustomer_Create_date"
                        Width="90"
                        Value="{Binding Path=Create_date, UpdateSourceTrigger=Default,
                                ValidatesOnDataErrors=True}"
                        Validation.ErrorTemplate="{StaticResource validationTemplate}"
                        IsEnabled="{Binding Path=Create_dateIsEnabled}"
                        IsReadOnly="{Binding Path=Create_dateIsReadOnly}"
                        VerticalAlignment="Center"
                        ToolTip="{Binding RelativeSource={RelativeSource Self},Path=(Validation.Errors), Converter={StaticResource errorConveter}}">
						<i:Interaction.Behaviors>
							<uiControls:SelectAllDateTimeEditorBehavior></uiControls:SelectAllDateTimeEditorBehavior>
						</i:Interaction.Behaviors>
                        <igWPF:XamDateTimeEditor.Style>
                            <Style>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Path=Create_dateIsFocused}" Value="true">
                                        <Setter Property="FocusManager.FocusedElement"
                                                Value="{Binding ElementName=ctlCustomer_Create_date}"></Setter>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self},Path=(Validation.HasError)}" Value="True">
                                        <Setter Property="DatePicker.Background">
                                            <Setter.Value>
                                                <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                                                    <LinearGradientBrush.GradientStops>
                                                        <GradientStop Offset="0.2" Color="WhiteSmoke" />
                                                        <GradientStop Offset="3" Color="Red" />
                                                    </LinearGradientBrush.GradientStops>
                                                </LinearGradientBrush>
                                            </Setter.Value>
                                        </Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </igWPF:XamDateTimeEditor.Style>
                    </igWPF:XamDateTimeEditor>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
</ResourceDictionary>

